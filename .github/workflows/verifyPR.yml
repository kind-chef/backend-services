on:
  pull_request:

jobs:
  integration-and-delivery:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      #- name: setup services
      #run: docker compose up -d --build

      - name: Prepare environment
        run: npm install

      - name: Execute linter
        run: yarn run eslint . --ext .ts

      #- name: docker ps
      #run: docker ps
      #- name: docker log
      #run: docker logs -f --until=2s kind-chef
      #- name: curl
      #run: curl -v htpp://localhost:8090/unassigned-workshops
      #- name: Run tests//
      #run: npm run test
