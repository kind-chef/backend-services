on:
  pull_request:

jobs:
  integration-and-delivery:
    runs-on: ubuntu-latest
    steps:
      - name: setup services
        run: docker compose up -d --build

      - uses: actions/checkout@v2

      - name: Prepare environment
        run: npm install

      - name: Execute linter
        run: yarn run eslint . --ext .ts

      - name: Run tests
        run: docker exec kind-chef npm run test
